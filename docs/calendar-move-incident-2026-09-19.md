# カレンダー移動の応答未確認（2026-09-19）

## 確認できたこと

- 利用者の報告・添付画像：2026年9月19日17:45 JST、求人を9月21日へ移動する際に「サーバーからの応答が届きませんでした」と表示。
- 本番 `event_audit`：17:40〜17:50 JSTの求人更新には、17:46:23.309751 JSTの求人 #1307 の日程変更が1件あった。開始日が9月20日から9月21日へ変わり、日付ラベルも更新されている。
- 本番 `jobs`：調査時点の #1307 は開始日9月21日、終了日なし、公開中。
- 17:45の操作が遅れて反映されたのか、その後の再操作が成功したのかは、この記録だけでは特定できない。
- 画像の文言は `rpcOutageKind` の `noResponse` 分岐。HTTP status 0またはfetchの通信失敗で表示され、サーバー停止を証明するものではない。
- REST通信は待機・本文受信を含め15秒で端末側から中断する。今回この期限切れが発生したかは確認できない。
- SupabaseのAPI通信ログを取得するツールは今回の接続に無く、ブラウザのログ画面も未ログインだった。当時の通信断・期限切れの直接原因は未確定。

出典：利用者提供画像 IMG_0051.png、本番Supabase `event_audit` / `jobs` / `pg_get_functiondef`、ソース `MyCalendar.jsx` / `requestTransport.js` / `rpcOutage.js`。確認日：2026-09-19 JST。

## 修正

- `move_job_dates` の応答が途切れた場合は、既存の `get_my_calendar_jobs` を読み直す。
- 同じ番号の本人求人が指定した開始日に存在すれば完了として扱い、取得した最新のカレンダーを表示する。
- 書き込みを自動再送しない。業務上の拒否（応募あり・過去日など）や権限エラーは、そのまま失敗として扱う。
- 照合でも確認できない場合は成功扱いにせず、取得できた最新の日程を表示する。読み込みも失敗した場合は手元の予定を維持する。
- 照合には6秒の中断期限を設け、「保存した日程を確認しています」を表示する。確認中の連打・別日選択・コピー・シート閉じを抑止する。
- 通信期限切れは `AbortError` のまま識別可能なメッセージを付ける。送信回数・通信枠・既存の15秒期限は変更しない。
- 移動失敗・照合の時刻、段階、HTTP状態、所要時間、端末側期限切れの別をコンソールに記録する。求人内容・求人番号・個人情報・認証情報は記録しない。

## 検証

`node --test scripts/calendar-move.test.mjs scripts/calendar-updates.test.mjs scripts/request-transport.test.mjs`：31件成功。

実コンポーネントをJSDOMと通信フィクスチャで操作：3件成功。
保存済みで応答のみ喪失／未保存／照合も通信失敗のそれぞれで、確認中表示、連打抑止、日付表示、警告の有無を確認した。

本番データを移動するテストは行っていない。通信断そのものの原因解消、実機の4G通信環境の再現を確認したものではない。
